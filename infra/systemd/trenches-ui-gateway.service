[Unit]
Description=Trenches UI Gateway
After=network.target

[Service]
WorkingDirectory=%h/trenches
EnvironmentFile=%h/trenches/.env
ExecStart=/usr/bin/pnpm --filter @trenches/ui-gateway start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target

